{% set name = "clmm" %}
{% set version = "1.16.1" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/clmm-{{ version }}.tar.gz
  sha256: 7dc9cc4ecb278935afdf691fb2d3635c9e04a335155a4da0f006bf9f2b848d8a

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - hatchling
    - pip
  run:
    - python >={{ python_min }}
    - astropy >=4,!=5.0
    - matplotlib-base
    - numpy >=1.17
    - scipy >=1.3
    - healpy
    - qp-prob

test:
  imports:
    - clmm
  requires:
    - pip
    - pyccl
    - python {{ python_min }}

about:
  home: https://lsstdesc.org/CLMM/
  summary: A comprehensive package for galaxy cluster weak lensing
  dev_url: https://github.com/LSSTDESC/CLMM
  license: BSD-3-Clause
  license_file: LICENSE

extra:
  recipe-maintainers:
    - eduardojsbarroso
    - beckermr
